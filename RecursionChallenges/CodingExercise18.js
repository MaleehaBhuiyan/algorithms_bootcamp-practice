//Write a recursive function called flatten which accepts an array and returns a new array with all values flattened 

function flatten(arr){
    var flatArr = arr.flat()
    canFlatten(arr, flatArr)
}

function canFlatten(arr, flatArr){
    if(arr != flatArr){
        
    }
}



flatten([[1],[2],[3]])