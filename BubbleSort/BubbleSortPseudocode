√1) Define a function called BubbleSort that takes in an array 
√2) Start looping from with a variable called i the end of the array towards the beginning 
√3) Start an inner loop with a variable called j from the beginning until i - 1 
√4) If arr[j] is greater than arr[j + 1], swap those 2 values 
√5) return the sorted array 